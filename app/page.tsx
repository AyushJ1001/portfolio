import { Confluence } from "@/components/Confluence";

// The hub's single origin and only door. The old single-scroll composition
// (Hero/Projects/Experience/…) is retired as the landing experience; its
// content is salvaged into world interiors, not preserved here.
export default function Home() {
  return <Confluence />;
}
